{
  "name": "mit-barn-billede-grabber",
  "version": "0.0.1",
  "description": "Web extension (chrome first) to fetch images, from your children's day care.",
  "scripts": {
    "start": "npm run dev",
    "dev": "webextension-toolbox dev chrome",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "jszip": "^3.1.5"
  }
}
